require "rails_helper"

RSpec.describe Vendor do
  it "does something", pending: "add some examples to (or delete) #{__FILE__}" do
    skip "model spec placeholder until something to test"
  end

  # deliberate choice NOT to test straight Rails model validations
end
